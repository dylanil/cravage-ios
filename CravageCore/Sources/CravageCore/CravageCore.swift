// Placeholder so the package builds and CI is green from the first push.
// The real modules land in delivery step 3 (FixedPoint, crypto, roster, RoundEngine, Transcript).
public enum CravageCore {
    /// Wire protocol version carried in every envelope as "v".
    public static let protocolVersion = 1
}
