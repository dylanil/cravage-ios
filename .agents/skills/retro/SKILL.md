---
name: retro
description: Run a close-of-session retrospective when the user wraps up or a milestone lands with no further work queued. Gather evidence, apply the resulting improvements, and leave a public-safe handoff.
---

Read and follow `../../../.claude/skills/retro/SKILL.md` relative to this directory.
That file is the canonical shared skill for Claude and Codex. For Codex-specific global skills
use `$CODEX_HOME/skills`; project skill behavior stays in `.claude/skills/`, with Codex discovery
entries in `.agents/skills/`. Shared repository rules in `CLAUDE.md` apply through `AGENTS.md`.
