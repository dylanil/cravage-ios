import SwiftUI

/// Placeholder until the step 4 mockups are approved and the real screens are built (step 5).
struct RootView: View {
    let coordinator: RoundCoordinator

    var body: some View {
        VStack(spacing: 12) {
            Text("Cravage").font(.largeTitle.bold())
            Text("Screens are being designed.").foregroundStyle(.secondary)
        }
        .padding()
    }
}
